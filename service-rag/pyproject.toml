[tool.poetry]
name = "service-rag"
version = "1.0.0"
description = "RAG query microservice"
authors = ["kurodayu23"]
packages = [{include = "app"}]

[tool.poetry.dependencies]
python = ">=3.11,<3.13"
rag-shared = {path = "../shared", develop = true}
torch = {version = "2.5.1+cpu", source = "pytorch-cpu"}
ollama = "^0.1.8"
fastapi = "^0.111.0"
uvicorn = {version = "^0.29.0", extras = ["standard"]}
langchain = {version = "^0.3.0", optional = true}
langchain-core = {version = "^0.3.0", optional = true}
langchain-community = {version = "^0.3.0", optional = true}

[tool.poetry.extras]
langchain_engine = ["langchain", "langchain-core", "langchain-community"]

[tool.poetry.group.dev.dependencies]
pytest = ">=8.2.0"
pytest-mock = "^3.14.0"
pytest-cov = "^5.0.0"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["..", "."]

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[[tool.poetry.source]]
name = "pytorch-cpu"
url = "https://download.pytorch.org/whl/cpu"
priority = "explicit"
